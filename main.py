from fastapi.middleware.cors import CORSMiddleware

from app.api.app import app
from app.core.settings import settings

print(settings.BEDROCK_INFERENCE_PROFILE_ID)

app.add_middleware(
    CORSMiddleware,
    allow_origins=[
        "http://localhost:5173",
    ],
    allow_credentials=True,
    allow_methods=["*"],
    allow_headers=["*"],
)

if __name__ == "__main__":
    import uvicorn

    uvicorn.run(
        "main:app",
        host="0.0.0.0",
        port=8000,
        reload=True,
    )